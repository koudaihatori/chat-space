.sidebar
  .sidetop
    %p.sidebar_user_name
      =current_user.name if user_signed_in?
      = link_to edit_user_registration_path, class: "fa" do
        %i.fa.fa-cog
      = link_to new_group_path, class: "fa" do
        %i.fa.fa-pencil-square
  .grouplist
    - current_user.groups.each do |group|
      =link_to group_comments_path(group) do
        %p.sidebar_group_name
          = group.name
        - unless group.comments.last.nil?
          %p.sidebar_group_chat= group.messages.last.body
        - else
          %p.sidebar_group_chat まだメッセージがありません

